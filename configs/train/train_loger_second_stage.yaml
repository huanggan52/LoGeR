# @package _global_

trainer: trainers.loger_trainer.Pi3Trainer

train:
  sequence_length: 128
  max_img_per_gpu: 128
  chunk_size_range: [4, 12]
  overlap_size_range: [1, 3]
  iters_per_epoch: 15000

  # low resolution
  resolution: 
    - [504, 280]

model:
  ckpt: null # <path-to-your-pretrain-checkpoint>